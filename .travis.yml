language: r
r:
  - release
  - devel

dist: bionic

cache:
  - packages

addons:
  apt:
    sources:
      - sourceline: 'ppa:ubuntugis/ubuntugis-unstable'
    packages:
      - libgeos-dev
      - libgdal-dev
      - libproj-dev
      - libudunits2-dev
      - libssl-dev
      - devscripts
